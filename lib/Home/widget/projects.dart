import 'package:flutter/material.dart';

Widget HomeProjects() {
  return Container(
      child: Column(
    children: const [

      /// Project Page Heading
      /// 
      Text(
        "Reelfolio Picks",
        style: TextStyle(
          color: Colors.white,
          fontSize: 16,
          fontWeight: FontWeight.w500,
        ),
      ),

      
    ],
  ));
}
